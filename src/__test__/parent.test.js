// import { render, screen } from "@testing-library/react";
// import ParentClass from "../components/ClassBasedComponents/ParentClass";
// import "@testing-library/jest-dom";


// //Describe is a grouping convention used to group similar test cases
// describe("Heading tests", () => {
//   test("Parent component should render", () => {
//     render(<ParentClass />);
//     const heading = screen.getByRole("heading");

//     //Assertion
//     expect(heading).toBeInTheDocument();
//   });


//   //Test is also called as it (test==it)  it is done for better readablity => it is more common 
//   // it is just an Alias for tests
//   //Description must start with "Should" a common convention followed by developer for tests
//   it("Should have only one heading", () => {
//     render(<ParentClass />);
//     const heading = screen.getAllByRole("heading");

//     expect(heading.length).not.toBe(2);
//   });
// });

// test("Parent component should have a button", () => {
//   render(<ParentClass />);

//   const button = screen.getByText("For test cases");

//   expect(button).toBeInTheDocument();
// });
it("one",()=>{
  
})
